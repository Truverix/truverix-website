# ── Supabase (Postgres, free tier) ─────────────────────────────────────────
# From your Supabase project: Settings → API.
# Use the service_role key here — NOT the anon/public key. The service_role
# key is what lets the server write to tables protected by Row Level
# Security. It must only ever live in Vercel's environment variables, never
# in client-side code or in this repo.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-key-here

# ── Admin login (for the /admin reporting dashboard) ──────────────────────
# Do NOT put a plain password here. Generate a bcrypt hash locally (see README
# "Step 6") and paste the hash — never the raw password — as the value below.
ADMIN_USERNAME=admin
# The password + hash below were freshly generated for you and are not used
# anywhere else. Log in with the password once, then change it later by
# generating a new hash the same way (see README "Rotating the admin password").
# Password: Quartz-Vortex-1463!H
ADMIN_PASSWORD_HASH=$2a$10$jdXSSQ2KpQzZ4tVpU1XWHOvhltWAAJ1hxWzZB2yQVTvg19gZVlvQa
ADMIN_JWT_SECRET=dbc4f21e4537904421cffd17ad88ef8b816c2f0561da2b99f4be549201e47530295825d8451734b36ebf36191f6936dd

# ── Cloudflare Turnstile (bot protection on the forms) ────────────────────
# From the Cloudflare dashboard → Turnstile → your widget.
TURNSTILE_SITE_KEY=0x0000000000000000000000
TURNSTILE_SECRET_KEY=0x0000000000000000000000

# ── Resend (email notification when a lead comes in) ──────────────────────
# Optional but recommended. From resend.com → API Keys.
RESEND_API_KEY=re_xxxxxxxxxxxx
NOTIFY_EMAIL_TO=sales@truverix.com
NOTIFY_EMAIL_FROM=noreply@truverix.com
